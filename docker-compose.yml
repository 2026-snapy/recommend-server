services:
  recommend-server:
    image: ghcr.io/2026-snapy/recommend-server:latest
    container_name: recommend-server
    networks:
      - snapy-net
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:5000:5000"

networks:
  snapy-net:
    external: true